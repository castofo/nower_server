en:
  errors:
    authorization:
      missing_header: 'missing authorization header'
      invalid_token: 'invalid authorization token'
  activerecord:
    errors:
      models:
        promo:
          attributes:
            start_date:
              already_started: 'cannot be changed once started'
        user:
          attributes:
            email:
              unregistered: 'is not registered'
        admin:
          attributes:
            email:
              unregistered: 'is not registered'
            privileges:
              not_array: 'is not an array'
              invalid: '%{privilege} is not allowed'
  admin_mailer:
    password_confirmation:
      subject: 'Account activation'
